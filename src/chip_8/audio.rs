extern crate sdl2; 

use sdl2::audio::{AudioCallback, AudioSpecDesired};
use std::time::Duration;

pub struct Audio
{
    sound_timer: i8,
    ///
}

impl Audio
{
    fn play_audio(sound_timer: &mut u8)
    {
        //
    }
}